<UserControl x:Class="AcePeerToPeerNetwork.Views.Controls.ListingsFeedControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AcePeerToPeerNetwork.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel
            Name="screenListingsFeed"
            Background="Blue">
        <ListView Name="listListingsFeed" Background="Gray">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Poster" DisplayMemberBinding="{Binding Poster.Username}"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
            <ListViewItem>Test Item</ListViewItem>
        </ListView>

        <Button 
            Background="Red"
            Content="Refresh"
            Name="btnListingFeedRefresh"
            Click="btnListingFeedRefresh_Click"/>

        <Button 
            Name="btnView"
            Content="View"
            Click="btnView_Click"/>
    </StackPanel>

</UserControl>
